# Global Context

## User
- Name: Spencer Duran
- Editor: neovim
- Shell: fish (not bash/zsh — write fish syntax for shell stuff)
- Terminal: Alacritty
- OS: macOS

## Obsidian Vault (Second Brain)
An Obsidian MCP server is connected to the vault at `~/vaults/second_brain`. Use MCP tools (read-note, create-note, edit-note, search-vault, etc.) to interact with notes, tasks, reference docs, and knowledge base content — regardless of the current working directory.

### Vault Structure (PARA-like)
- `000 Inbox/` — capture & triage; `Unfiled/` for quick captures needing processing
- `200 Areas/` — ongoing areas of responsibility, numbered by priority
  - `0 MLG/` (work), `1 NinjaStrat/`, `2 Trading/`, `3 Parenting/`, `4 Health/`, `5 Music/`, `6 Stoicism/`, `7 Cooking/`, `8 Politics/`, `9 Home/`
  - `claude/reference/` — Claude Code reference docs
  - `Tax Notes/`, `Reading List.md`
  - Projects live inside their respective area (e.g., `200 Areas/0 MLG/Projects/`), not in a separate top-level folder
- `300 Resources/` — reference material, knowledge base, scripts
  - `Chat Prompting/`, `Data Governance/`, `Mortgage/`, `PARA Theory/`, `Technical Documentation/`, `Scripts/`, `Library/`, `Reading Notes/`, `Misc/`
  - `Templates/` — Daily Note, Meeting, MOC, Quick Capture, Weekly Review
- `400 Daily Notes/` — daily journal (GTD tasks, meds tracking, daily log)
- `500 Archives/` — completed projects, old notes
- `_System/` — dashboard, GTD views, vault infrastructure

### Note Conventions
- Frontmatter fields: `type` (MOC, meeting, Note, Source, tweet), `tags`, `priority`, `banner`
- MOC files act as folder indexes (auto-generated by Zoottelkeeper plugin)
- Tasks use inline tags (`#mlg`, `#ninjastrat`, etc.) and priority icons (high to low)
- Dataview/DataviewJS powers dashboard queries and task aggregation

**Tweet note schema** (`_tweets/YYYY-MM-DD @handle.md`):
```markdown
---
type: tweet
source: https://x.com/handle/status/ID
author: "@handle"
author_name: Display Name
date: YYYY-MM-DD
media: video | image | text
---

> tweet text

[Video: X seconds]
![thumb](URL)

<details>
<summary>Transcript</summary>
transcript
</details>

Topics: [[Topic MOC]]
Tags: #subtopic-tag
```

### Knowledge Organization System

**Two complementary structures — not redundant:**
- **Subject subfolders** (Trump/, Israel/, Immigration/): group notes about one concrete entity. Use when 3+ notes exist on a single subject. 1-2 notes on a topic stay at area root.
- **Topic MOCs** (Culture War & Identity.md, Government Accountability.md, etc.): cross-cutting thematic backlink hubs. Stay at area root always. Remain sparse — let backlinks accumulate naturally, add synthesis only when backlink panel exceeds ~10 entries.

**Inline linking pattern for content notes:**
```
Topics: [[Culture War & Identity]] · [[Immigration & Demographics]]
Tags: #anti-woke #demographic-replacement
```
- `Topics:` line — wikilinks to topic MOC files, placed at end of each entry/note
- `Tags:` line — inline hashtags for subtopic granularity, placed immediately after Topics:
- Both lines are discoverable via Obsidian backlinks panel and tag pane respectively

**`_tweets/` atomic note pattern** — one file per tweet/bookmark, inside area folders:
- File path: `_tweets/YYYY-MM-DD @handle.md` (collision suffix: `YYYY-MM-DD @handle 2.md`)
- Use tweet frontmatter schema above
- Each note has `Topics:` + `Tags:` inline lines for backlinks and tag pane discovery
- Underscore prefix sorts to top of file explorer, signals source/intake material
- Queryable by Dataview (`type: tweet`), linkable as vault nodes, exact QMD hits
- Migration script: `200 Areas/8 Politics/_tweets/migrate.py` (adaptable per area)

**Subtopic tag taxonomy for 8 Politics** (reference when adding new tweet notes):
- Culture War & Identity: `#race-stats` `#anti-woke` `#gender-trans` `#demographic-replacement` `#corporate-dei`
- Immigration & Demographics: `#open-borders` `#deportation` `#welfare-state` `#migrant-crime`
- Elite Power & Deep State: `#deep-state-ops` `#epstein-network` `#bioweapons` `#conspiracy-qanon` `#israel-intel` `#uniparty`
- Government Accountability: `#waste-fraud` `#doge` `#federal-bloat`
- Tax & Economic Policy: `#tax-reform` `#tariffs` `#income-tax` `#deficit-spending`
- Election Integrity: `#voter-fraud`

**When adding new content to an area with this system:**
1. Assign primary + secondary Topics: wikilinks based on content
2. Add subtopic Tags: using the area's taxonomy (or extend taxonomy if genuinely new subtopic)
3. Create one atomic note per tweet in the area's `_tweets/` folder
4. Entries with insufficient content to classify → omit rather than force a bad tag
5. Optionally add `See also:` line with 1-2 wikilinks if tweet directly references an existing vault note

### Task System
- Tasks are created in Daily Notes and Project MOCs
- `#mlg` tagged tasks show separately on the dashboard
- Priority icons: highest to lowest
- Dashboard at `_System/Dashboard.md` aggregates all active tasks

### Where to Save New Content
- Quick captures / unsorted: `000 Inbox/Unfiled/`
- Area-specific work: `200 Areas/<area>/` (projects go in `<area>/Projects/`)
- Reference / knowledge base: `300 Resources/` in the appropriate subfolder
- Claude Code docs: `200 Areas/claude/reference/`
- Daily notes: `400 Daily Notes/` using Daily Note Template

### When Writing Notes
- Always include Obsidian frontmatter (`type`, `tags` at minimum)
- Use markdown, keep formatting compatible with Obsidian
- Match existing conventions in the target folder

## Dotfiles
- Repo: `~/Documents/dotfiles`
- Manages: fish, neovim, hammerspoon, alacritty, tmux
- Fish functions go in `fish/functions/` as individual `.fish` files
- Commit messages: keep simple (e.g., "dotfilescommit")

## Session Memory
- VAULT_DIR is ~/vaults/claude_memory — dedicated vault for Claude session history
- Full session exports: ~/vaults/claude_memory/Claude-Sessions/
- QMD-indexed extracts: ~/vaults/claude_memory/Notes/Projects/claude-sessions-qmd/
- QMD collections: `sessions` (session extracts), `notes` (second_brain vault)
- Use /recall for temporal queries (yesterday, last week) or topic search
- Use /sync-claude-sessions to export, resume, annotate sessions
- second_brain vault is read-only for Claude — search via QMD but don't write to it

## Context Management

Context is your most important resource. Proactively use subagents (Agent tool) to keep exploration, research, and verbose operations out of the main conversation.

**Default to spawning agents for:**
- Codebase exploration (reading 3+ files to answer a question)
- Research tasks (web searches, doc lookups, investigating how something works)
- Code review or analysis (produces verbose output)
- Any investigation where only the summary matters

**Stay in main context for:**
- Direct file edits the user requested
- Short, targeted reads (1-2 files)
- Conversations requiring back-and-forth
- Tasks where user needs intermediate steps

**Rule of thumb:** If a task will read more than ~3 files or produce output the user doesn't need to see verbatim, delegate it to a subagent and return a summary.

## Tmux Layout
- Pane %0: Claude Code (this session)
- Pane %1: Preview shell — use to show documents/output to the user
  - To preview a file: `tmux send-keys -t %1 'q' C-m 'preview "path/to/file"' C-m`
  - Send `q` first to exit any active previewer before starting a new one
  - `preview` uses markitdown + glow — supports md, docx, xlsx, pptx, pdf, html

## Preferences
- Keep responses concise
- Don't over-engineer — minimal changes, no unnecessary abstractions
- No emojis unless asked
- When creating shell scripts or functions, use fish syntax
- Prefer editing existing files over creating new ones
- **Code navigation**: Prefer the LSP tool (go-to-definition, find-references, diagnostics) over grep/ripgrep for finding symbols, tracing call hierarchies, and locating definitions. Fall back to Grep (ripgrep) for text/pattern searches where LSP doesn't apply.

## CocoIndex (Semantic Code Search)
The `cocoindex-code` MCP server provides semantic code search via a `search` tool. It uses AST-based indexing with local embeddings to find code by meaning, not just text matching.

**Use cocoindex `search` instead of Grep/Glob when:**
- Exploring an unfamiliar codebase and you don't know exact names (e.g., "find authentication logic", "where are database connections configured")
- A keyword search would miss relevant results because the code uses different terminology
- You need to understand how a concept is implemented across multiple files

**Still use Grep/Glob/LSP when:**
- You know the exact symbol name, string, or pattern
- You need precise text matching (regex, literal strings)
- You're doing go-to-definition or find-references on a known symbol

## Writing Standards (stop-slop)

When writing any prose — notes, annotations, docs, messages — apply these rules. Full skill at `~/.claude/skills/stop-slop/SKILL.md`.

**Core rules:**
1. Cut filler phrases — no throat-clearing openers, emphasis crutches, adverbs
2. Break formulaic structures — no binary contrasts ("not X, it's Y"), no dramatic fragmentation
3. Active voice only — every sentence needs a human subject doing something
4. Be specific — no vague declaratives, no lazy extremes ("always", "every", "never")
5. Put the reader in the room — "you" beats "people", specifics beat abstractions
6. Vary rhythm — mix sentence lengths, two items beat three, no em dashes
7. Trust readers — state facts directly, skip softening and hand-holding
8. Cut quotables — if it sounds like a pull-quote, rewrite it

**Quick checks before delivering prose:** adverbs? passive voice? inanimate subject doing a human verb? Wh- sentence opener? throat-clearing setup? three consecutive same-length sentences? paragraph ending with a punchy one-liner? em dash?
